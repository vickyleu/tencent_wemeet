name: tencent_wemeet
description: A new Flutter project.
version: 0.0.1
homepage:

environment:
  sdk: '>=2.18.4 <3.0.0'
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  pigeon: ^4.2.5

flutter:
  plugin:
    platforms:
      android:
        package: com.example.tencent_wemeet
        pluginClass: TencentWemeetPlugin
      ios:
        pluginClass: TencentWemeetPlugin
      macos:
        pluginClass: TencentWemeetPlugin
      windows:
        pluginClass: TencentWemeetPluginCApi
